// Optional local overrides. Copy to config.local.js if needed.
// config.local.js is gitignored and will not be committed.
window.INSIGHT_HARNESS_CONFIG = {
  // Optional: override public proxy while developing.
  // proxyUrl: "http://127.0.0.1:8787/api/gemini",
  liveMode: true,
};
